<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title|default('OkTicket App') }}</title>

    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      body {
        font-family: "Inter", sans-serif;
        background-color: #0b1220;
        color: #ffffff;
      }
    </style>
  </head>
  <body class="min-h-screen flex flex-col">
    <!-- NAVBAR -->
    <nav
      class="flex justify-between items-center px-6 py-4 border-b border-white/10 bg-[#0b1220]/90 backdrop-blur-sm"
    >
      <a href="/" class="text-xl font-bold text-white">OkTicket</a>

      <div class="flex gap-4 items-center">
        <a href="/" class="hover:text-blue-400">Home</a>
        <a href="/dashboard" class="hover:text-blue-400">Dashboard</a>
        {% if session.user %}
          <a
            href="/auth/logout"
            class="px-3 py-2 bg-red-600 rounded text-white hover:bg-red-700"
          >
            Logout
          </a>
        {% else %}
          <a
            href="/auth/login"
            class="px-3 py-2 bg-blue-600 rounded text-white hover:bg-blue-700"
          >
            Login
          </a>
          <a
            href="/auth/signup"
            class="px-3 py-2 border border-blue-600 text-blue-400 rounded hover:bg-blue-600/20"
          >
            Sign Up
          </a>
        {% endif %}
      </div>
    </nav>

    <!-- MAIN CONTENT -->
    <div class="flex-1 p-6">
      {% block content %}{% endblock %}
    </div>

    <!-- FOOTER -->
    <footer class="text-center py-6 text-sm text-gray-400 border-t border-white/10">
      © {{ "now"|date("Y") }} OkTicket. All rights reserved.
    </footer>
  </body>
</html>
